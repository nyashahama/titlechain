{
  "name": "titlechain",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/portal"
  ],
  "scripts": {
    "build": "npm run build --workspace @titlechain/portal",
    "dev": "npm run dev --workspace @titlechain/portal",
    "lint": "npm run lint --workspace @titlechain/portal",
    "test": "npm run test --workspace @titlechain/portal",
    "test:watch": "npm run test:watch --workspace @titlechain/portal"
  }
}
